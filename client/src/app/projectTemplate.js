import * as mock from "./mock/tables/tables.mock";

const projectTemplate = {
  windows: mock.WINDOWS_TABLES,
  walls: mock.WALLS_TABLES,
  energy: mock.ENERGY_TABLES,
  bgv: mock.BGV_TABLES,
  roof: mock.ROOF_TABLES,
  floor: mock.FLOOR_TABLES,
  zones: mock.ZONES_TABLES,
  lighting: mock.LIGHTING_TABLES ,
};

export { projectTemplate };
